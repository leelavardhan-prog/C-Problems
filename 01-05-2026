#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n = 5;

    int *arr1 = (int*)malloc(n * sizeof(int));
    int *arr2 = (int*)calloc(n, sizeof(int));

    for(int i=0;i<n;i++)
    {
        arr1[i] = i+1;
        printf("%d ", arr1[i]);
    }

    free(arr1);
    free(arr2);
}
