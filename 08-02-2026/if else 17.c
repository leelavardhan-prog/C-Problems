#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    if(n>=10&&n<=20){
        printf("number within a range");
    }
    else{
        printf("not within range");
    }
    
    return 0;
}