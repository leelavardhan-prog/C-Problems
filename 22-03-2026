#include <stdio.h>
int main(){
    int i, j;
    int num = 1;
    int n = 4;
    for(i = n; i >= 1; i--){
        for(j = 1; j <= i; j++){
            printf("%d ", num);
            num++;
        }
        printf("\n");
    }
    return 0;
}
